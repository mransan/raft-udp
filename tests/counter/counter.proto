option (int32_type) = int_t;

message AppData {
  required int32 increment  = 1;
  required int32 process_id = 2;
    // for logging purposes
}

message AppResult {
  required int32 from  = 1; 
  optional int32 to = 2; 
}
